- winner = @judge.winner

#judge-result
  %h3
    あなたが選んだのは
  = render :partial => "photos/basic", :as => :photo, :object => winner, :locals => { :show_name => true }
  %p
    %strong
      = "Score: %d" % winner.rate

= render :partial => "judges/voters", :locals => { :first => @first, :second => @second }
